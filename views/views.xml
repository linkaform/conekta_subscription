<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="conekta_subscriptions.list">
      <field name="name">conekta_subscriptions list</field>
      <field name="model">conekta_subscriptions.conekta_subscriptions</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="conekta_subscriptions.action_window">
      <field name="name">conekta_subscriptions window</field>
      <field name="res_model">conekta_subscriptions.conekta_subscriptions</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="conekta_subscriptions.action_server">
      <field name="name">conekta_subscriptions server</field>
      <field name="model_id" ref="model_conekta_subscriptions_conekta_subscriptions"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="conekta_subscriptions" id="conekta_subscriptions.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="conekta_subscriptions.menu_1" parent="conekta_subscriptions.menu_root"/>
    <menuitem name="Menu 2" id="conekta_subscriptions.menu_2" parent="conekta_subscriptions.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="conekta_subscriptions.menu_1_list" parent="conekta_subscriptions.menu_1"
              action="conekta_subscriptions.action_window"/>
    <menuitem name="Server to list" id="conekta_subscriptions" parent="conekta_subscriptions.menu_2"
              action="conekta_subscriptions.action_server"/>
    -->
  </data>
</odoo>